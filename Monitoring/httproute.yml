apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  namespace: monitoring
spec:
  parentRefs:
    - name: devboard-gateway                     # <-- your Envoy Gateway "Gateway" resource name
      namespace: devboard-app  # <-- namespace where that Gateway lives                
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /monitoring
      backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80